# 配图素材说明

## 封面图

- `wechat-cover.svg`：公众号封面/头图用。

使用方式（任选其一）：

1. 用浏览器打开 `wechat-cover.svg`，截图保存为 PNG。
2. 用任意支持 SVG 导出的工具打开并导出 PNG（建议宽度保持 1200）。

## 建议截图（你自己补）

建议你在发布前补 3 张截图，效果会明显更“爆款”：

- `OpenClawManager` 仪表盘（能看到启动状态）
- `.env` 只露出字段名（遮住值）
- 运行成功日志（或状态为 Running 的证明）

